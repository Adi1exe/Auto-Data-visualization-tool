# Auto-Data-visualization-tool

step 1: download the repo (either clone or download zip file)
step 2: open the main-folder in vscode
step 3: open terminal
-- `python -m venv venv`
-- `venv\Scripts\activate`
-- `pip install -r requirements.txt`
step 4: open app.py and click on run
step 5: terminal madhe link yeil
-- `http:127.0.0.1:500`
